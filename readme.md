## simple server for testing webhooks payload

To run protect, run this command (node.js required)
```
npm install
```

```
node index.js
```

Then config your endpoint to this url http://localhost:3000/webhook